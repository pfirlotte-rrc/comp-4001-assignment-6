apiVersion: v1

kind: Service
metadata:
  name: transactions
spec:
  type: ClusterIP
  selector:
    matchLabels:
      app: transactions
  ports:
    - port: 3000
      name: http
      targetPort: http